spring:
  application:
    name: authorization
  datasource:
    url: jdbc:postgresql:localhost:5432
    username: postgres
    password: postgres
  thymeleaf:
    cache: false
server:
  port: 1440
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1337/eureka/
